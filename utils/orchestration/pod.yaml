apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: nginx
    sidecar.istio.io/inject: "true"
  name: mission-app
  namespace: mission
spec:
  containers:
  - image: nginx:1.27.0
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}